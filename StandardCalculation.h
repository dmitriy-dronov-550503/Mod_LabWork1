#pragma once
#include <iostream>
#include <vector>
using namespace std;

class StandardCalculation
{
public:
	static double ExpectedValue(vector<double> xs);
	static double Variance(vector<double> xs);
};

